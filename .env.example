# ModelScope API Key
# 从 ModelScope 获取你的 API Key: https://www.modelscope.cn/
MS_API_KEY=your_modelscope_api_key_here

# 嵌入模型配置 (可选，使用本地模型或 API)
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# arXiv 查询配置
ARXIV_MAX_RESULTS=50
ARXIV_FETCH_INTERVAL_HOURS=24

# 数据库路径
DATABASE_PATH=./arxiv_papers.db
CHROMA_DB_PATH=./chroma_db

# 邮件服务配置
# 支持Gmail、QQ邮箱、163邮箱等SMTP服务
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
FROM_EMAIL=your_email@gmail.com
ADMIN_EMAIL=kaiqinglei3@gmail.com

# Gmail配置示例:
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# 需要开启"两步验证"并生成"应用专用密码"

# QQ邮箱配置示例:
# SMTP_SERVER=smtp.qq.com
# SMTP_PORT=587
# 需要开启SMTP服务并生成授权码

# 163邮箱配置示例:
# SMTP_SERVER=smtp.163.com
# SMTP_PORT=465
